import React from "react";
import style from "./Ingredients.module.css";

const BreadBottom = () => {
  return (
    <div className={`${style.BreadBottom} ${style.Bread}`}>Bread Bottom</div>
  );
};

export default BreadBottom;
