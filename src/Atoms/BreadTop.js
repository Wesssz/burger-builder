import React from "react";
import style from "./Ingredients.module.css";

const BreadTop = () => {
  return <div className={`${style.BreadTop} ${style.Bread}`}>Bread Top</div>;
};

export default BreadTop;
